engines:
  eslint:
    enabled: true
    channel: 'eslint-9'
    config:
      config: 'eslint.config.mjs'

checks:
  similar-code:
    enabled: false
  method-complexity:
    config:
      threshold: 10

ratings:
  paths:
    - '**.js'
